[package]
name = "tectonic-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tectonic_bundles = "*"
jni = "0.21.1"
tectonic = { version = "*", default-features = false } # Adjust based on the tectonic version
